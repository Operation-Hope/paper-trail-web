# Paper Trail Frontend Environment Variables

# Backend API URL (optional - leave empty to use proxy)
# Development: Empty (proxied through Vite dev server)
# Production: Empty (proxied through Express server)
# VITE_API_BASE_URL=

# The actual backend is configured in:
# - Development: vite.config.ts (proxies to localhost:5001)
# - Production: server.js (proxies to paper-trail-api.railway.internal)
